{
	"name": "@travel-anchor/react-spa",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"check": "tsc --noEmit",
		"dev": "vite",
		"build": "tsc -b && vite build",
		"lint": "eslint .",
		"preview": "bun run build && vite preview",
		"deploy": "bun run build && wrangler deploy",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"react": "^19.1.1",
		"react-dom": "^19.1.1",
		"react-router": "^7.9.4"
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "^1.13.16",
		"@types/react": "^19.1.10",
		"@types/react-dom": "^19.1.7",
		"@vitejs/plugin-react": "^5.0.0",
		"typescript": "catalog:",
		"vite": "^7.1.2",
		"wrangler": "^4.45.1"
	}
}
